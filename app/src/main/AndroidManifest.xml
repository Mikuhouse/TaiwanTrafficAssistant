<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.taiwantrafficassistant">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:permission=""
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".bus.BusListActivity"
            android:label="@string/bus_list_activity_label">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".bus.BusRouteSearchActivity"
            android:label="@string/bus_route_search_label">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".bus.BusListActivity" />
        </activity>
        <activity
            android:name=".test.TestGithubRepoApiActivity"
            android:label="@string/test_github_repo_api_label">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".bus.BusListActivity" />
        </activity>
        <activity
            android:name=".test.TestPtxApiActivity"
            android:label="PtxRailWayApiTest">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".bus.BusListActivity" />
        </activity>
        <activity
            android:name=".bus.BusTimeOfArrivalActivity"
            android:label="公車到站時間" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".bus.BusNearbyStopOnMapActivity"
            android:label="@string/title_activity_bus_nearby_stop_on_map" />
        <activity android:name=".bus.BusFavorateActivity" />
        <activity android:name=".mrt.MrtListActivity" />
        <activity android:name=".mrt.MrtMapWebviewActivity" />
        <activity android:name=".test.TableLayoutTestActivity"></activity>
    </application>

</manifest>